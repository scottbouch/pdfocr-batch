# pdfocr-batch
Batch process a directory of plain PDFs, to PDFs with OCR layer.

Place in a directory of PDFs, make executable, and run with ./pdfocr-batch.sh command.

Temporary directories of intermediate pdf and png files are left in place presently while in development.
